[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ql-relational"
version = "0.1.0"
description = "Prime Agent faculty over source-locked EpiLogos QL/MEF and Wiki operations"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
