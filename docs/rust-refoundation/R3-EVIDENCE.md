# R3 — Realised agency and runtime evidence

Serial PR #61 follows accepted/reverified R2 main `05e440f4628bbc40eca30f2cabb869a76e99ddd9`. No later migration branch exists.

The single acting agent authored and tested source tree `9fae3f750011e2c16ab035a11e538d0afe63cc83`. It introduces the typed realised/actualisation library, managed/discovered locus driver, ordinary executor-neutral acting loop and immutable extraction corpus. `R3-RUNTIME.md` records the design and its limits. Original Node product source and public schemas remain unchanged.

The source capsule's actual decoded SHA-256 is `ccc66965e29ed3d89513ec0630471ee0bac99513c058dd32c6d7a8165b03c18a`. Its initial header mistakenly named a different digest. Workflow 34436829037 / job 102743543847 correctly refused that mismatch before importing source or running R3 tests. All four source parts were independently retrieved and compared byte-for-byte to the authored candidate; they were unchanged. The header and ancestor check were corrected without weakening the payload, original-blob or exact-tree checks. That failed workflow is not acceptance evidence.

Corrected workflow **34437114232** passed exact-tree admission and native/Node/Skill checks. Artifact **10136570534** has ZIP SHA-256 `a276bbf5671d4bddad1f312648070c7e957decdd3c0a088297a0040d313be22f`. Its published candidate commit is `6a76a95aa3593ee778f06da96bf6397e2cbf370e`. Independent Git-bundle comparison confirmed that its only difference from the tested source tree was the temporarily retained importer workflow. This final publication removes that workflow and all capsule material before maintained read-only verification.

Executed native evidence: formatting; locked workspace/all-target Clippy with warnings denied; 34 native integration tests; one compile-fail identity doctest; all 119 constitutional and 44 realised/actualisation public parity cases; and all 29 frozen original-runtime host-call/event/outcome cases exercised in the native extraction tests. The unchanged original product passed all 219 native tests, 599 public JSON cases and 67 fresh state/effect/CLI scenarios. Foundation/Deep/Prime/epistemic tests remain active in maintained CI; 84 passed in the authoring run. Native Skills verification passed.

Lifecycle tests include managed/discovered modes, ordinary project World without Factory, rich external body refs, exact admission/grant/bounds/lineage, false identity/observation refusal, unsupported control with unchanged state, body/session/process/model/material continuity, observed relocation then interrupt/resume/cancel, terminal refusal, and Return without inferred reception/Recognition. Generic LoopRuntime/RuntimeHost and the bounded ordinary loop are now A-disposition native APIs. The original JS implementation remains temporary A residue for unmigrated research consumers, not a specimen exemption; R6 owns consumer migration and retirement.

All this is **D evidence** over explicitly supplied hosts, observers and fixtures. There is no live provider P, owner-machine M, human H, current suite-wide C, physical cancellation or philosophical cognition claim. Actualisation admission does not launch a process. Driver observation never allocates a Workcell, selects a model or installs a harness. Formal QL semantics remain the QL-MEF owner's responsibility.

Merge requires subsequent green read-only checks on this final readable-source PR, on both Linux and macOS. Re-read and verify the resulting main before beginning R4. #58 remains open through the R10 / O:I #213 S7 physical and human proving boundary.
