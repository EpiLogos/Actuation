# Epi-Logos agent: Prime–QL constitution and differentiated faculties

**Standing:** owner-commissioned architecture lock and implementation/test assignment, 22 September 2026. This publication does not claim the composed runtime, default-mode selection, installed providers or capability gains have been demonstrated.

**Execution:** [Actuation #107](https://github.com/EpiLogos/Actuation/issues/107), inside the existing research programme; [O:I #220](https://github.com/EpiLogos/O-I/issues/220) coordinates integration and [#65](https://github.com/EpiLogos/O-I/issues/65) supplies C0–C5 proving. Domain authority: [QL-MEF companion](https://github.com/EpiLogos/QL-MEF/blob/main/docs/integrations/epi-logos/EPI-LOGOS-AGENT-ARCHITECTURE.md), existing #135/#201 and registered capability field. Application contract: [Epi-Logos agent mode](https://github.com/EpiLogos/O-I/blob/main/docs/experience/EPI-LOGOS-AGENT-MODE.md).

**Authored clarification:** the owner's final distinction is explicit: “the #0/1 level is distinct from #0”; “the 0/1 is literally the agent loop and its ql formed tooling”. The same commission asks that this become the default agent/harness option in Expressions and Technē when the existing Epi-Logos mode button is selected. Earlier descriptions that placed the whole agent in #0, or omitted #0 by starting the faculty map at #1, are superseded.

## 1. Purpose and local notation

The 4/5/0 mental/personal pole completes the same instrument whose physical 1/2/3 body is already being integrated. M4 Nara is the hinge: the physical event is received, interpreted and participated in at a particular person's locus. M5 Epii investigates, teaches, operates and develops that world. M0 Anuttara is its language-bearing ground. These are domain relations, not claims of phenomenal subjectivity or measured therapeutic benefit.

The complete agent is organised as follows:

```text
#0/1  CONSTITUTED AGENT
       Prime body + QL recurrence + relational orientation
       + QL-formed operative/reflective tools + situated native World
       |
       +-- #0  Anuttara / Bimba / Jev / Ananda-related reading / EBM basis
       +-- #1  formal and topological intelligence, including TDA
       +-- #2  semantic graph operations, analytics and relational learning
       +-- #3  symbolic/multimodal representation and process intelligence
       +-- #4  Nara contextual NLP, elemental/archetypal activity and dialogue
       +-- #5  Epii pedagogy, Logos-cycle Return and improvement of practice
```

`#0/1 != #0`. The containing constitution is not a seventh peer faculty, another kernel root, a new coordinate registry or a service to be installed beside its faculties. These local architectural positions do not reparent existing Bimba nodes. `M0-i′`, `Mi/Mi′` and `M5′ operating upon Mi` are related but distinct: a graph-engagement mode, the domain instrument, and its developmental capacity. QL-MEF's companion gives their technological content and source provenance.

S4′ is **Anima**; S5′ is **Aletheia**. M4/M4′ is **Nara**; M5/M5′ is **Epii**. Same-position affinity is not identity or exclusive technical ownership. Ta-Onta supplies the existing S′ SDK/API over O:I; Actuation does not redefine those organs.

## 2. #0/1: compose the real body, not another harness

Consume the current Rust implementation, not retired JavaScript commands copied from an old research README:

- `crates/actuation-research/src/prime.rs` and `prime_run.rs`: native Prime execution, real child lineage, source lock, bounded recursion and deliberate refinement;
- `faculty.rs` and `owner.rs`: source-qualified native QL faculty, owner binding and operation evidence;
- `relational.rs` and `policy.rs`: experimental QL recurrence, residue, stipulation, allowance and determination machinery over the shared RuntimeHost;
- `toolset.rs`: the QL-formed operative and reflective supplies;
- `ql-relational-system-prompt.md`: the owner-authored Relational Logos orientation;
- `experiments/ql-runtime/prime/`, `experiments/native-research/` and `experiments/model-types/`: retained conditions, bridges, question sets and evidence.

Prime's own recursive execution and QL recurrence are not interchangeable schedulers. Preserve the experiments that ask whether recurrence operates within one locus, across an AgenticComposition, or recursively at both scales. Do not replace Prime's recursion with a second loop and report that as native Prime evidence.

The product composition binds an enduring Agent, situated Agency, subject, task/NOW, capabilities, authority, actual model/provider and material body. Replacing a process, model or AgentSession does not remint Nara or Epii. AIKit resolves the composition; Actuation constitutes and observes Agency; Workcell supplies material execution; Central owns source/NOW continuity; Factory owns commissioned development.

Relational Logos is a versioned orientation about source, representation, perspective, context and Return. It is not a per-turn demand to narrate hidden reasoning or recite six headings. Select applicable Skills/Methods and actual sources rather than inject every architecture document into every child.

### Tool arities and experimental independence

The current supplies are:

| Supply | Actual current specimen | Research distinction |
|---|---|---|
| `ql-tagged` / 4 | list_files, read_file, write_file, run_tests | QL office descriptions on ordinary world operations, no explicit loop verbs |
| `ql-toolset` / 6 | the four plus situate and close | explicit situation and check-gated Return |
| `ql-eight` / 8 | the four plus reflect_being, reflect_becoming, reflect_knowing, reflect_resonant_frames | operative work plus selected reflective instruments, without the two return verbs |
| `ql-twelve` / 12 | the six plus all six Night tools, including reflect_full_text and reflect_right_frame | full supplied operative/reflective reference form |

Four specimen commands are not four exhaustive domain capabilities and do not map one-to-one onto positions. A tool count is not the count of M domains, MEF lenses, archetypal numbers, grammar forms or Agents. The integrated product must expose real source, graph, QL, Expression and Technē operations through its existing repertoire; it cannot remain limited to a filesystem test fixture.

**Initial integration candidate:** the full twelve-tool reference form, composed with the relational orientation and native domain faculties. This is a proposed first engineering/test configuration, not evidence that twelve outperforms four, six or eight. Record the actual admitted arity/adapter expansion; later selection changes require a source-qualified comparison. Never silently substitute an arity and still report the previous condition.

Preserve prompt-led, tool-only, native-Prime, explicit-Return and continual conditions independently. The toolset-only experiment deliberately does not require the full relational prompt. Combining them in the product does not retroactively change those experiments or attribute every observed gain to the tools.

P/P′ are outward and returning readings of the same work. A reflective call can supply a useful reading without mechanically moving the active position. Current toolset closure is not delegated to Jev: objective checks and the selected explicit closure policy remain native. New semantic assistance must not become a universal classifier veto.

### Real descendants

A child receives the same eligible faculty definitions through supported Prime/native inheritance, but a bounded task, source view and effect authority. Prove requested -> resolved -> delivered -> invoked separately; a parent advertising a Python faculty does not prove child loading. No role-play children and no private parent/verifier corpus copied into every descendant.

Bind the persistent assigned feature-line worktree and file claims before invocation. Ordinary workers and descendants cannot create another worktree, clone or editable full-source copy to escape a conflicting claim. Verify the actual material/native boundary, not just instruction compliance; retain legitimate bounded scratch, build outputs and committed test exports. Serialize shared Git/index/checkout/commit operations. A session replacement reuses the continuing task and allocation.

Use current AIKit cheapest-eligible policy for bounded test children where compatible with the task; record actual child model, provider, budget, assistance and justified escalation. Do not hard-code a remembered cheap model or purchase access to pass a trial.

## 3. #0: full Anuttara–Jev–EBM basis

This faculty is explicitly present inside the constitution. It contains access to the complete source-qualified 109-node Anuttara syntax, its formulations and declared relation content, native QL operations, and the current Jev/Ananda/EBM experimental composition. It is not only C′ metadata, an embedding search, a bag of labels or another relational system prompt.

`experiments/model-types/ARCHITECTURE.md` v2 is the instrument-design basis, read with actual newer evidence. Retain its distinctions:

- Jev classifies expressions/states/acts against the actual syntax, archetypal structures, R-factors and virtue readings;
- declared relation content is first-class; node similarity is not a substitute for the relation itself;
- Ananda provides its own numerical-relational reading at **M1-2**; participation in #0's composed basis does not reparent or repurpose it as a universal semantic verifier;
- the EBM supplies compatibility evidence over the source-defined virtue and general relational fields; energy is not truth, human worth, permission or automatic acceptance;
- Rust owns mechanical recurrence, bounds and effects.

Keep all distinct twelves and other source cardinalities qualified. Missing reader evidence is not a map defect. Older mark-table questions are reconciled with current owner source rather than answered by invented symbols. No graph sync, source backfill, canonical mutation or training is authorised by selecting the mode.

Consume the existing v2 design and experiments instead of rebuilding Vāk. Reconcile `hybrid-loop/EBM-GATE-DESIGN.md` and older handoffs where their scoring object, calibration or energy convention predates v2. Start new influence on live decisions in shadow/observational mode; any bounded second-look/refusal policy needs source-defined scope, calibration and recovery. An unavailable advisory instrument discloses abstention rather than manufacturing a verdict; missing mandatory authority/formal checks still prevent the corresponding effect.

## 4. Full #1–#5 capacities, not only training methods

The QL-MEF companion is authoritative for the domain mapping. Actuation binds real callable operations and evidence for:

- **#1:** formal/structural inquiry, metric/filtration-qualified persistent homology and topology-sensitive assessment, alongside mathematical articulation/CPT/RAG;
- **#2:** Cypher/APOC traversal, neosemantics/RDF/SHACL and explicitly supported inference, GDS analytics, typed node/relation representations and graph ML;
- **#3:** symbolic and multimodal source/glyph/scene/temporal representations, cross-modal retrieval, exact transformations and learnable operative pathways;
- **#4:** protected contextual NLP and span-bearing elemental/archetypal observations, confidence distinct from contribution strength, native activity reduction/decay and separately governed voice refinement;
- **#5:** Logos-cycle pedagogy, T/C/T′/C′ consumption, evidence-backed practice improvement and independent later uptake; etymological Atelier is a specialised method, not the whole cycle.

Use existing capability matrices and native owners. A listed algorithm is not an installed faculty; every operation retains source, scope, supported input/output, revision, effect/authority and evidence standing. An unavailable research extension stays visible without blocking every ordinary act. Full lane closure nevertheless accounts for all required branches: one successful #0 demo does not prove the whole agent.

## 5. Native context and current-world continuity

Consume [AIKit #388](https://github.com/EpiLogos/ai-kit/issues/388) and [Jev/Redis NOW integration](https://github.com/EpiLogos/O-I/blob/main/docs/experience/JEV-REDIS-NOW-INTEGRATION.md): general Jev transport and typed invocation, Central/BKMR/source continuity, participant-specific preparation, actual native delivery and durable Return. Domain question sets remain with their Skills/Methods and QL sources; do not create another Jev credential store, cache, Wiki, NOW, model roster or memory daemon.

A prepared view can include current #0 readings, relevant #1–#5 resources and surrounding work. Pin the basis delivered to the actual act. A newer observation does not silently replace an in-flight act's source, Expression or candidate. Recheck disclosure at preparation and delivery; raw Nara material and verifier expectations do not leak into siblings or an external provider.

Use pithy native NOW handover: actual delta, exact sources/evidence, unresolved or uncertain effects and next action. A fresh worker follows those references and the current prepared view; the parent does not retell the whole investigation. Preserve Direct versus Factory lineage and one personal Day over the intended Workcell horizons.

## 6. Epi-Logos mode is an operative selection

Within **both Expressions and Technē**, selecting the existing Epi-Logos mode selects this Prime–QL agent/body composition as the default option. This is mode-local, not a global replacement of every user's harness. It adds a particular way of attending, operating and returning within the same selected subject/scene, not another application or merely a visual theme.

Nara remains the foreground dialogical identity where the domain calls for it; Epii supplies deeper enrichment/development through the existing Ta-Onta path. Both can consume the shared constitution with appropriate scope and latency. Prime is not automatically a realtime voice transport; preserve native voice/provider contracts and actual supported interruption. S4′ Anima composes participation; S5′ Aletheia receives actual performance and Return.

Persist authored mode/profile preference separately from effective runtime state. On entry, resolve and disclose the actual Agent, model, Prime build, QL owner, prompt/tool/faculty revisions, NOW/SessionSpace and readiness. A selected chip is not successful activation. Never label a generic fallback as the active QL body. Missing required capability produces an intelligible unavailable/repair choice; optional unavailable faculties remain separately disclosed. Preserve an explicit user override and show how it differs from the default.

Changing the mode must not mutate canon, start training or auto-refinement, create a new worktree, acquire credentials, remint an enduring Agent or hot-swap an active attempt. Use a supported turn/session boundary with explicit pending-work disposition. Mode-off restores the legitimate prior ordinary selection while retaining source and work. Re-entry/restart recovers the same subject and truthful composition, not stale injected context. The detailed O:I note owns UI/proving branches.

## 7. Bounded implementation and joined proof

This is a **parallel mode-quality lane in the current single harmonisation**, not another cutover. It may be performed in the lead's existing main feature tree with file claims and serialized shared mutations. Remote PR branches do not prescribe new local worktrees. Coordinate runtime/faculty changes with the current writer; do not overwrite in-flight `policy.rs` or duplicate the #388 transport. Do not reset post-origin test evidence.

1. Bind the actual composition and capability inventory from current code and source; publish missing native contracts without reopening completed kernel/AW work.
2. Make the selected body and its #0–#5 faculties discoverable through AIKit and invocable through real Prime/native children; add source/contract/negative tests.
3. Join the existing mode selection and Ta-Onta receiving route on the current UI; test both Expressions and Technē.
4. Run one useful source-bound inquiry with actual #0 operation, differentiated contribution where warranted, native output and Return; exercise each required faculty branch in appropriate coverage episodes.
5. Replace a worker and resume from minimal NOW continuation, then demonstrate warranted knowledge/practice improvement on unfamiliar later input.

Under #65 C0 binds intent/capabilities; C1 establishes actual source/build/install/running and separate actor/verifier inputs; C2 performs the encounter; C3 exercises descendants, changes and interruption; C4 preserves failure and repairs; C5 independently replays and retains actual human assessment where required. The lane can be constructed alongside general Jev/Redis work. It does not make advanced QL/TDA/training a prerequisite for #388's first ordinary Factory/NOW episode or waive the existing foundation condition before broad fan-out.

### Required discriminating cases

- Default resolution in both host modes; explicit override; mode-off and re-entry; unavailable Prime/QL; optional Jev/EBM loss; no hidden generic substitution.
- Exact full-syntax reading and native operation; disagreement between instruments; stale relation/embedding basis; wrong/malformed provider answer; out-of-scope scoring abstention.
- Real child inheritance and bounded authority; conflicting file claim; independently exercised denial of extra-worktree creation; ordinary editing/testing still works.
- Same source/Expression/scene through dialogue, action, interruption, rejected proposal and late Return; a state acknowledgement without actual native effect fails.
- Protected Nara source, unknown NLP result, confidence versus weight, activity versus identity, decay/replay and external-egress restriction.
- A Logos-cycle Return consumed into a useful native source/practice, then used by a fresh participant without expected-answer coaching; a near-miss must not trigger that practice.

Compare ordinary, prompt-only, tool/recurrence and relevant instrument configurations on an identified common source/task basis with bounded budgets. Separate apparatus conformance from capability-effect measurement. Include repeated trials/held-out cases, full model/retrieval/verification overhead, assistance and negative/null results. A possible product differentiator is a hypothesis until these comparisons establish what improved.

## 8. Source provenance and return

This lock follows the owner's conversation through the final #0/1-versus-#0 correction. It consumes the original M0′ representation/processing and M4′ activity/M5 Logos sources through QL-MEF's source map, rather than reducing Epii-on-X to the compact training registry. Historical technology locations retain provenance but do not redefine current native ownership.

Inspection bases for this publication: Actuation `8459d503542e3707dc03919ba6f467abb9f60e09`, QL-MEF `2bcdc78c2f97ce13312bf96011bd18bea7b73ea5`, O:I `fdb7958d16c94cc4538cc4208bf62f8192486ec2`. These are source bases, not an installed compatibility receipt.

Return exact native operations, source/profile/tool versions, actual invocation and child lineage, selected/delivered/used context, material generations, mode UI evidence, failures/repairs and independent replay to #107/#220/#65 and QL-MEF #201. Source publication and CI do not report a live default agent, successful provider experiment or human Recognition. No machine mutation is performed by this documentation commission.

## 9. Implemented acting-body candidate — 2026-09-22

The runtime candidate is published in [#110](https://github.com/EpiLogos/Actuation/pull/110). `crates/actuation-research/src/epi_agent.rs` constitutes the **#0/1** body against the QL owner constitution and refuses a mismatched #0–#5 readback. `application.rs` exposes `epi-agent.body` and `epi-agent.run`; the existing Prime recursion remains the scheduler.

The existing inherited `ql-relational` Skill now carries actual #0–#5 calls and leaves the same native faculty receipts at parent and child loci. It prefers an installed `QL_BIN` with an explicit `QL_OWNER_REVISION`; the source-checkout cargo path remains only the matched research fallback. `faculty.rs` verifies each QL invocation's position and operation instead of accepting an arbitrary wrapper result. The #3 branch also delegates `techne_reading(target)` to QL-MEF's existing production `ql techne reading` adapter; the native owner admits that exact command and the resulting `ql-techne-reading` receipt is attributed to Mahāmāyā rather than treating a catalogue entry as execution.

For application use, `actuation-epi-prime` is the installable Prime JSONL-RPC launcher. It binds the admitted QL binary/revision, relational Skill, research/faculty bridge and RLM depth into Prime's native process. Provider/model are a paired explicit override: when AIKit has selected one, they are delivered and Prime must read them back; when no AIKit override is authored, Prime's own configured model is observed rather than invented by Actuation. Mode selection never authorises Continual refinement.

The twelve-tool form remains the initial reference research supply. The current product transport truth is narrower: domain faculties reach Prime through the inherited Skill/faculty bridge until the Prime transport itself advertises them as first-class tool descriptors. The 4/6/8/12, prompt-only and tool-only conditions therefore remain real comparative conditions instead of being rewritten around the product candidate.

Cloud evidence proves the Prime structural apparatus, inherited Skill compilation, native research boundaries, deep runtime and CLI/build surfaces. Credentialled Prime inference, inexpensive-child model choice, installed material/worktree restriction, Jev/EBM use and human experience are not inferred from those gates; they remain explicit joined/local acceptance conditions.
